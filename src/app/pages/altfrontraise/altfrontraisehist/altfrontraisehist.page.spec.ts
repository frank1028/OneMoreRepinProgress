import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AltfrontraisehistPage } from './altfrontraisehist.page';

describe('AltfrontraisehistPage', () => {
  let component: AltfrontraisehistPage;
  let fixture: ComponentFixture<AltfrontraisehistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AltfrontraisehistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
