import { ComponentFixture, TestBed } from '@angular/core/testing';
import { AlthammercurlhistPage } from './althammercurlhist.page';

describe('AlthammercurlhistPage', () => {
  let component: AlthammercurlhistPage;
  let fixture: ComponentFixture<AlthammercurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(AlthammercurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
