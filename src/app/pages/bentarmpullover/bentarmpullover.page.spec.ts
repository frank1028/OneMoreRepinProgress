import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BentarmpulloverPage } from './bentarmpullover.page';

describe('BentarmpulloverPage', () => {
  let component: BentarmpulloverPage;
  let fixture: ComponentFixture<BentarmpulloverPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BentarmpulloverPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
