import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BentarmpulloverhistPage } from './bentarmpulloverhist.page';

describe('BentarmpulloverhistPage', () => {
  let component: BentarmpulloverhistPage;
  let fixture: ComponentFixture<BentarmpulloverhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BentarmpulloverhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
