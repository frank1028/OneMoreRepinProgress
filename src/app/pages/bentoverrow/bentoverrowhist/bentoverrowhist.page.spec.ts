import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BentoverrowhistPage } from './bentoverrowhist.page';

describe('BentoverrowhistPage', () => {
  let component: BentoverrowhistPage;
  let fixture: ComponentFixture<BentoverrowhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BentoverrowhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
