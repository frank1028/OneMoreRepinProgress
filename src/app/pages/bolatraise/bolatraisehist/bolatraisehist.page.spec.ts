import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BolatraisehistPage } from './bolatraisehist.page';

describe('BolatraisehistPage', () => {
  let component: BolatraisehistPage;
  let fixture: ComponentFixture<BolatraisehistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(BolatraisehistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
