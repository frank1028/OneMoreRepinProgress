import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ClosebbcurlhistPage } from './closebbcurlhist.page';

describe('ClosebbcurlhistPage', () => {
  let component: ClosebbcurlhistPage;
  let fixture: ComponentFixture<ClosebbcurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ClosebbcurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
