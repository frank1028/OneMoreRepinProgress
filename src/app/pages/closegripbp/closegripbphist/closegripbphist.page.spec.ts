import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ClosegripbphistPage } from './closegripbphist.page';

describe('ClosegripbphistPage', () => {
  let component: ClosegripbphistPage;
  let fixture: ComponentFixture<ClosegripbphistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ClosegripbphistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
