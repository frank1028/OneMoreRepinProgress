import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DbtricepsexthistPage } from './dbtricepsexthist.page';

describe('DbtricepsexthistPage', () => {
  let component: DbtricepsexthistPage;
  let fixture: ComponentFixture<DbtricepsexthistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DbtricepsexthistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
