import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DbuprightrowhistPage } from './dbuprightrowhist.page';

describe('DbuprightrowhistPage', () => {
  let component: DbuprightrowhistPage;
  let fixture: ComponentFixture<DbuprightrowhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DbuprightrowhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
