import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DeclinepresshistPage } from './declinepresshist.page';

describe('DeclinepresshistPage', () => {
  let component: DeclinepresshistPage;
  let fixture: ComponentFixture<DeclinepresshistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DeclinepresshistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
