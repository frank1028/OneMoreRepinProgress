import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InclinebenchhistPage } from './inclinebenchhist.page';

describe('InclinebenchhistPage', () => {
  let component: InclinebenchhistPage;
  let fixture: ComponentFixture<InclinebenchhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InclinebenchhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
