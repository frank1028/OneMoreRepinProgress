import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InclinecurlhistPage } from './inclinecurlhist.page';

describe('InclinecurlhistPage', () => {
  let component: InclinecurlhistPage;
  let fixture: ComponentFixture<InclinecurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InclinecurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
