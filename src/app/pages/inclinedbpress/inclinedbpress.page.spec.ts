import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InclinedbpressPage } from './inclinedbpress.page';

describe('InclinedbpressPage', () => {
  let component: InclinedbpressPage;
  let fixture: ComponentFixture<InclinedbpressPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InclinedbpressPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
