import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InclinedbpresshistPage } from './inclinedbpresshist.page';

describe('InclinedbpresshistPage', () => {
  let component: InclinedbpresshistPage;
  let fixture: ComponentFixture<InclinedbpresshistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InclinedbpresshistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
