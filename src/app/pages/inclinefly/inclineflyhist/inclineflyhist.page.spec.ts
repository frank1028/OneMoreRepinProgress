import { ComponentFixture, TestBed } from '@angular/core/testing';
import { InclineflyhistPage } from './inclineflyhist.page';

describe('InclineflyhistPage', () => {
  let component: InclineflyhistPage;
  let fixture: ComponentFixture<InclineflyhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(InclineflyhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
