import { ComponentFixture, TestBed } from '@angular/core/testing';
import { KctricepsexthistPage } from './kctricepsexthist.page';

describe('KctricepsexthistPage', () => {
  let component: KctricepsexthistPage;
  let fixture: ComponentFixture<KctricepsexthistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(KctricepsexthistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
