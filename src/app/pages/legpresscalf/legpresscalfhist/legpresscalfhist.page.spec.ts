import { ComponentFixture, TestBed } from '@angular/core/testing';
import { LegpresscalfhistPage } from './legpresscalfhist.page';

describe('LegpresscalfhistPage', () => {
  let component: LegpresscalfhistPage;
  let fixture: ComponentFixture<LegpresscalfhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(LegpresscalfhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
