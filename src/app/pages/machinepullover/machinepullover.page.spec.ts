import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MachinepulloverPage } from './machinepullover.page';

describe('MachinepulloverPage', () => {
  let component: MachinepulloverPage;
  let fixture: ComponentFixture<MachinepulloverPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MachinepulloverPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
