import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MachinepulloverhistPage } from './machinepulloverhist.page';

describe('MachinepulloverhistPage', () => {
  let component: MachinepulloverhistPage;
  let fixture: ComponentFixture<MachinepulloverhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MachinepulloverhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
