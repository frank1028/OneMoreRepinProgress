import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OhwristcurlhistPage } from './ohwristcurlhist.page';

describe('OhwristcurlhistPage', () => {
  let component: OhwristcurlhistPage;
  let fixture: ComponentFixture<OhwristcurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OhwristcurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
