import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OnearmohexthistPage } from './onearmohexthist.page';

describe('OnearmohexthistPage', () => {
  let component: OnearmohexthistPage;
  let fixture: ComponentFixture<OnearmohexthistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OnearmohexthistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
