import { ComponentFixture, TestBed } from '@angular/core/testing';
import { OnearmrowhistPage } from './onearmrowhist.page';

describe('OnearmrowhistPage', () => {
  let component: OnearmrowhistPage;
  let fixture: ComponentFixture<OnearmrowhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(OnearmrowhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
