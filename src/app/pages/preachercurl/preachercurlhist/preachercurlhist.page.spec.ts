import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PreachercurlhistPage } from './preachercurlhist.page';

describe('PreachercurlhistPage', () => {
  let component: PreachercurlhistPage;
  let fixture: ComponentFixture<PreachercurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PreachercurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
