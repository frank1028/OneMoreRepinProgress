import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ReversedbcurlhistPage } from './reversedbcurlhist.page';

describe('ReversedbcurlhistPage', () => {
  let component: ReversedbcurlhistPage;
  let fixture: ComponentFixture<ReversedbcurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ReversedbcurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
