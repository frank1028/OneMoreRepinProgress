import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RopepushdownhistPage } from './ropepushdownhist.page';

describe('RopepushdownhistPage', () => {
  let component: RopepushdownhistPage;
  let fixture: ComponentFixture<RopepushdownhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RopepushdownhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
