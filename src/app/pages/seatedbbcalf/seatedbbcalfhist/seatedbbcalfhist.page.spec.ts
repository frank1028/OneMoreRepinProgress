import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeatedbbcalfhistPage } from './seatedbbcalfhist.page';

describe('SeatedbbcalfhistPage', () => {
  let component: SeatedbbcalfhistPage;
  let fixture: ComponentFixture<SeatedbbcalfhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeatedbbcalfhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
