import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeateddbpressPage } from './seateddbpress.page';

describe('SeateddbpressPage', () => {
  let component: SeateddbpressPage;
  let fixture: ComponentFixture<SeateddbpressPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeateddbpressPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
