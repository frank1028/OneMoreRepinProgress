import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeateddbpresshistPage } from './seateddbpresshist.page';

describe('SeateddbpresshistPage', () => {
  let component: SeateddbpresshistPage;
  let fixture: ComponentFixture<SeateddbpresshistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeateddbpresshistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
