import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeatedlegcurlhistPage } from './seatedlegcurlhist.page';

describe('SeatedlegcurlhistPage', () => {
  let component: SeatedlegcurlhistPage;
  let fixture: ComponentFixture<SeatedlegcurlhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeatedlegcurlhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
