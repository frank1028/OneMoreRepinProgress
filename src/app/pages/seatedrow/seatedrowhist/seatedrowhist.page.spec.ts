import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SeatedrowhistPage } from './seatedrowhist.page';

describe('SeatedrowhistPage', () => {
  let component: SeatedrowhistPage;
  let fixture: ComponentFixture<SeatedrowhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SeatedrowhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
