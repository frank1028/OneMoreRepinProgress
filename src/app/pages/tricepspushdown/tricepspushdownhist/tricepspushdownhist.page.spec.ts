import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TricepspushdownhistPage } from './tricepspushdownhist.page';

describe('TricepspushdownhistPage', () => {
  let component: TricepspushdownhistPage;
  let fixture: ComponentFixture<TricepspushdownhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TricepspushdownhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
