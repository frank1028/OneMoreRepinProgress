import { ComponentFixture, TestBed } from '@angular/core/testing';
import { UhpulldownhistPage } from './uhpulldownhist.page';

describe('UhpulldownhistPage', () => {
  let component: UhpulldownhistPage;
  let fixture: ComponentFixture<UhpulldownhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(UhpulldownhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
