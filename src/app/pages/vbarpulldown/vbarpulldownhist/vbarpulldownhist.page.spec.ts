import { ComponentFixture, TestBed } from '@angular/core/testing';
import { VbarpulldownhistPage } from './vbarpulldownhist.page';

describe('VbarpulldownhistPage', () => {
  let component: VbarpulldownhistPage;
  let fixture: ComponentFixture<VbarpulldownhistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(VbarpulldownhistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
