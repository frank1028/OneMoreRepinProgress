import { ComponentFixture, TestBed } from '@angular/core/testing';
import { WidegripbphistPage } from './widegripbphist.page';

describe('WidegripbphistPage', () => {
  let component: WidegripbphistPage;
  let fixture: ComponentFixture<WidegripbphistPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(WidegripbphistPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
